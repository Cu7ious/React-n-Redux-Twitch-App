import { changeTab } from '../actions';

const initialState = {
  tab: 1
};

export default function twitchApp (state, action) {

  if (typeof state === 'undefined') {
    return initialState;
  }

  

  return state;

}
